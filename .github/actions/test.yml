name: 'lvCICD-TEST'
description: 'Test LabVIEW CICD'
inputs:
  # who-to-greet:  # id of input
  #   description: 'Who to greet'
  #   required: true
  #   default: 'World'
outputs:
#   random-number:
#     description: "Random number"
#     value: ${{ steps.random-number-generator.outputs.random-id }}
runs:
  using: "composite"
  steps:
    - run: $GITHUB_ACTION_PATH/ViServerPort.ps1
      shell: powershell